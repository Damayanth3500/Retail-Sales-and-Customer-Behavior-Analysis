# Dataset files

The project analysis is designed for the supplied `customer_data.csv` and `sales_data.csv` files.

The two CSV files are large source datasets, so this repository currently contains the SQL schema, analysis, documentation, and project README. Add the CSV files to this folder if you want the complete raw dataset hosted in GitHub.
